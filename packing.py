class CrateWork:
    def __init__(self, large, depth, weight);      #Medidas necessárias para definção da obra e caixa
    self.large = large                             #Comprimento
    self.depth = depth                             #Largura
    self.weight = weight                           #Altura

    def works (self, code);
    self.works = works

    def crate (self, crate);
    self.crate = crate
